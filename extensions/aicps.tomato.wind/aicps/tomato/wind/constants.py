# temp holder for all constants we want to be referenced

CONTROLLER_SUFFIX = "_Controller"
DEFAULT_MIN_ANGLE = -15.5
DEFAULT_MAX_ANGLE = 15.5 

# X (pitch) and Z (roll) placeholders. Deliberately tighter than Y:
# no biomechanical basis yet, same caveat as Y originally had, but a stem
# twisting/tipping sideways is a smaller natural motion than swinging.
DEFAULT_MIN_ANGLE_X = -5.0
DEFAULT_MAX_ANGLE_X = 5.0
DEFAULT_MIN_ANGLE_Z = -5.0
DEFAULT_MAX_ANGLE_Z = 5.0
